import { reactive } from "vue";

new Vue({
    el: "#app",
    data: {
      
      menuItems: ["Home", "About", "Services", "Contact"],
    },
    // Eventuali metodi o altri attributi che potresti necessitare
  });